Installing the Merkle Tree (Hash Tree) implementation

The Merkle Tree (Hash Tree) implementation is a software library intended for
linking to other software, it is therefore not installed. See README for
details on how to build and use the Merkle Tree (Hash Tree) implementation.
